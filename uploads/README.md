This folder stores uploaded product and accessory images for the Adal Uganda Express backend.

The Express uploader saves image files under public/uploads and records metadata in PostgreSQL if DATABASE_URL is configured.
